# UriPack: uribrowserdocker

Thin Markpact — capabilities from [`uribrowser/uribrowserdocker/manifest.yaml`](uribrowser/uribrowserdocker/manifest.yaml).
Handlers: existing ``python://`` refs (code in `uribrowser/`, not duplicated here).

Generated by `urisys/scripts/generate_pack_markpacts.py`.

```yaml markpact:pack
apiVersion: urisys.io/v1
kind: UriPack
metadata:
  id: uribrowserdocker
  version: 0.1.0
  language: python
description: browser:// capability pack.
schemes:
- browser
capabilities:
- id: browser-status
  uri: browser://{session}/query/status
  kind: query
  operation: browser.status
  handler: python://uribrowserdocker.handlers:status
  side_effects: false
  approval: not_required
- id: browser-page-open
  uri: browser://{session}/page/command/open
  kind: command
  operation: browser.page.open
  handler: python://uribrowserdocker.handlers:open_page
  side_effects: true
  approval: required
- id: browser-page-open
  uri: browser://{session}/page/open
  kind: command
  operation: browser.page.open
  handler: python://uribrowserdocker.handlers:open_page
  side_effects: true
  approval: required
- id: browser-page-dom
  uri: browser://{session}/page/query/dom
  kind: query
  operation: browser.page.dom
  handler: python://uribrowserdocker.handlers:get_dom
  side_effects: false
  approval: not_required
- id: browser-page-screenshot
  uri: browser://{session}/page/command/screenshot
  kind: command
  operation: browser.page.screenshot
  handler: python://uribrowserdocker.handlers:screenshot
  side_effects: true
  approval: required
- id: browser-page-screenshot
  uri: browser://{session}/page/active/screenshot
  kind: command
  operation: browser.page.screenshot
  handler: python://uribrowserdocker.handlers:screenshot
  side_effects: true
  approval: required
- id: browser-form-submit
  uri: browser://{session}/form/command/submit
  kind: command
  operation: browser.form.submit
  handler: python://uribrowserdocker.handlers:submit_form
  side_effects: true
  approval: required
- id: browser-social-publish_post
  uri: browser://{session}/social/command/publish-post
  kind: command
  operation: browser.social.publish_post
  handler: python://uribrowserdocker.handlers:publish_post
  side_effects: true
  approval: required
policy:
  default: deny_mutations_without_approval
runtime:
  default_environment: real
  supports:
  - mock
  - local
  - docker
  expose:
  - pack
  - service
  - flow
  - interface
  - adapter
```

```yaml markpact:run
scheme: browser
default: flow
modes:
- pack
- service
- flow
- interface
- adapter
service:
  port_hint: 8792
  path: /uri/call
flow:
  ids:
  - browser-smoke
uses: []
adapter:
  call: POST /uri/call
  events: GET /events
```

```yaml markpact:tests
tests:
- id: browser.status_query
  uri: browser://default/query/status
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: browser.status
- id: browser.page.open_command
  uri: browser://default/page/command/open
  context:
    approved: true
    dry_run: true
    environment: mock
  expect:
    ok: true
    operation: browser.page.open
  payload: {}
```

```yaml markpact:flow id=browser-smoke
flow:
  id: browser-smoke
  description: Smoke browser:// routes (generated from manifest).
defaults:
  approved: true
  dry_run: true
do:
- browser://default/query/status
- browser://default/page/command/open: {}
- browser://default/page/open: {}
```

```markdown markpact:docs
## Unpack → `.markpact/`

```bash
cd uribrowser
export TELLMESH_ROOT=/home/tom/github/tellmesh
PACK=markpacts/uribrowserdocker.markpact.md
urisys markpact run "$PACK" --as flow --approve --dry-run
urisys markpact run "$PACK" --as pack
urisys markpact run "$PACK" --as service --port 8792
urisys markpact run "$PACK" --as interface
urisys markpact run "$PACK" --as adapter
```

- Source manifest: `uribrowser/uribrowserdocker/manifest.yaml`
- Requires: `pip install -e uribrowser` or `TELLMESH_ROOT` for python:// handlers
```
